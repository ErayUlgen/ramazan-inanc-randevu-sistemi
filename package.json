{
  "name": "ramazan-inanc-randevu",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run start:dev",
    "db:up": "docker compose up -d --wait postgres",
    "db:migrate": "npm --prefix backend run db:migrate",
    "db:status": "npm --prefix backend run db:status",
    "db:seed": "npm --prefix backend run db:seed",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "test": "npm --prefix backend run test -- --runInBand",
    "readiness": "npm --prefix backend run readiness",
    "backup:verify": "npm --prefix backend run backup:verify --"
  }
}
